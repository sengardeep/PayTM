# Copy this file to .env and replace placeholder values.
# Keeping secrets in env variables prevents accidental leaks in source control.
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/paytmdb
JWT_SECRET=replace-with-a-long-random-secret
PORT=3000
